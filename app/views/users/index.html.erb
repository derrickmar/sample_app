<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>

<ul class="users">
  <%= render @users %>
  <!-- render knows to iterate through the pagination of users. It --
    -- looks for _users.html.erb because @users is of class User. -->
</ul>

<%= will_paginate %>
